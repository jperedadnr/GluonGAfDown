buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'org.javafxports:jfxmobile-plugin:1.3.4'
    }
}

apply plugin: 'org.javafxports.jfxmobile'

repositories {
    jcenter()
    maven {
        url 'http://nexus.gluonhq.com/nexus/content/repositories/releases'
    }
}

mainClassName = 'com.gluonhq.gaf.down.GluonGAfDown'

dependencies {
    compile 'com.gluonhq:glisten-afterburner:1.1.0'
}

jfxmobile {
    downConfig {
        version = '3.2.2'
        // Do not edit the line below. Use Gluon Mobile Settings in your project context menu instead
        plugins 'accelerometer', 'compass', 'display', 'lifecycle', 'magnetometer', 'pictures', 'statusbar', 'storage'
    }
    android {
        manifest = 'src/android/AndroidManifest.xml'
    }
    ios {
        infoPList = file('src/ios/Default-Info.plist')
        forceLinkClasses = [
                'com.gluonhq.gaf.down.**.*',
                'com.gluonhq.**.*',
                'javax.annotations.**.*',
                'javax.inject.**.*',
                'javax.json.**.*',
                'org.glassfish.json.**.*'
        ]
    }
}
